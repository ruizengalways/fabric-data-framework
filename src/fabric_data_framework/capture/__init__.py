"""Capture package. Import semantics and implementations from explicit submodules.

Examples: ``capture.semantic_contracts``, ``capture.patterns``, ``capture.cdc``,
``capture.watermark``, ``capture.full`` and ``capture.onboarding``.
"""
