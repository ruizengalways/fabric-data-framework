"""Canonical relational control-plane package.

Import concrete contracts from explicit submodules such as ``schema``,
``repository`` or ``sqlalchemy_repository``. The package root deliberately
does not recreate the former flat-module API.
"""
